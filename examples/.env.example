# 📖 Complete configuration template for mqtt-typed-client examples

# 🔒 SECURITY NOTE:
# - examples/.env is committed to git and should contain ONLY safe defaults
# - For secrets, create examples/.env.local (ignored by git)
# - Never commit passwords, API keys, or personal data!

# MQTT Broker Configuration
MQTT_BROKER=mqtts://localhost:8883              # Local TLS broker (requires: cd dev && docker-compose up)
# MQTT_BROKER=mqtt://localhost:1883             # Local plain broker
# MQTT_BROKER=mqtt://broker.hivemq.com:1883     # Public HiveMQ broker
# MQTT_BROKER=mqtt://broker.mqtt.cool:1883      # Alternative public broker

# MQTT Authentication (⚠️ Use .env.local for real credentials!)
# MQTT_USERNAME=your_username                   # Use .env.local for this!
# MQTT_PASSWORD=your_password                   # Use .env.local for this!

# Tracing Configuration
RUST_LOG=info                                   # Standard logging
# RUST_LOG=debug                               # Verbose logging
# RUST_LOG=warn                                # Minimal logging
# RUST_LOG_DISABLE=1                           # No logging
